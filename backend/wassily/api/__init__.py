"""HTTP API: read-only JSON routes and a Server-Sent Events stream.

Import the factory from ``wassily.api.app``; this package stays import-light so
the serializers can be used offline without FastAPI installed.
"""
